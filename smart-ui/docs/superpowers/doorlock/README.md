# 门锁 Web 后台复刻规划

状态：第一版开发基线，尚未开始页面实现。

## 目标

第一版在 Smart 的管理后台中复刻旧门锁系统 smart-ui 的管理操作。这里的“一摸一样”指管理员完成业务时看到的入口、筛选条件、字段、按钮、弹窗、确认、校验、禁用、列表、详情页签、导出和结果反馈保持可逐项对照；不以重新设计的工作台替代旧页面。

复刻的是用户操作契约，不是旧接口、旧缺陷或不安全的数据展示：

- 新页面统一调用未来的 smart-lock 北向 API，并集中封装在 smart-ui/src/api/lock/；禁止页面直连旧门锁服务或协议桥。
- 密码明文、完整卡号、指纹模板、网关密钥、原始 TCP 报文和旧系统的硬编码身份参数不复刻。
- 旧页面已知的未完成“指纹采集”占位不应被当作可上线能力；设备或协议未验证的功能保持不可用且说明原因。

## 文档导航

| 文档 | 用途 |
|---|---|
| [legacy-reference.md](legacy-reference.md) | 旧构建产物、动态路由和证据边界。 |
| [surface-index.md](surface-index.md) | 8 个 Web 复刻入口、旧组件和目标组件映射。 |
| [surfaces/](surfaces/) | 每个页面组的字段、动作、状态和验收清单。 |
| [implementation-plan.md](implementation-plan.md) | 前端可拆分 WBS、依赖和实施顺序。 |
| [parity-acceptance.md](parity-acceptance.md) | 操作对版证据、回归与上线准入。 |

## 与其他模块的边界

| 归属 | 职责 |
|---|---|
| smart-ui | Web 页面、路由、动态菜单适配、资源码前端校验、请求封装、E2E 与操作对版证据。 |
| smart-h5 | 员工本人自助门锁页面；不承载后台管理操作。详见 [smart-h5 门锁文档](../../../../smart-h5/docs/superpowers/doorlock/README.md)。 |
| smart-lock | 领域规则、北向 API、权限与审计、可靠命令、数据迁移。 |
| smart-bridge-lock | 网关连接、协议、命令投递、设备回执和上行事件。 |

## 第一版硬约束

1. 不新建与旧后台并列的“命令中心”或“迁移大屏”来替换旧管理员日常入口；这些运维信息可作为详情、任务结果或后续迭代能力，不能挤掉旧操作。
2. 不直接覆盖当前 smart-ui 中的宿舍门锁页面。先以旧页面清单逐项补齐并完成对版，再决定重复入口的合并、下线或重定向。
3. 所有危险操作仍应保留旧版的显式确认与禁用语义；新后端返回“已受理/待设备确认”时，页面不得展示为物理操作已完成。
4. 旧 Web 菜单是登录后动态获取的。旧部署的真实菜单名、层级、URL 和资源码尚未从运行态取得，属于 OPEN-008 的证据项。
