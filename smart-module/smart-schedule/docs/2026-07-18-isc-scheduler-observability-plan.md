# ISC 调度日志可观测性实施计划

1. 新增 ISC 载荷格式化工具：脱敏手机号/证件号，截断人脸和超长载荷。
2. 替换权限配置主链路中无上下文的状态日志，记录可检索的任务字段和状态转换。
3. 为 ISC 分发请求、响应、失败和异常记录分发上下文、任务摘要和耗时。
4. 统一 ISC 定时任务的开始、完成、异常和跳过日志；补充离线设备任务的处理汇总。
5. 为载荷格式化添加单元测试，执行 `smart-schedule` 定向测试和 Maven 编译。
