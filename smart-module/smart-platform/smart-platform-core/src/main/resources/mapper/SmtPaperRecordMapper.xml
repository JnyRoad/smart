<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tce.smart.platform.core.mapper.SmtPaperRecordMapper">


	<select id="getPaperStaffTotal" parameterType="java.lang.Integer" resultType="java.lang.String">
		select BADGE from SMT_PAPER_RECORD
		where PAPER_ID=#{id}
		group by BADGE
	</select>

	<select id="getQuestionStatistics" parameterType="java.lang.Integer" resultType="com.tce.smart.platform.core.vo.QuestionStatisticsVO">
		select id, title,type from SMT_QUESTION where id in (
		select QUESTION_ID from SMT_PAPER_RECORD
		where PAPER_ID =#{id}
		group by QUESTION_ID)
		and type in (0,1)
	</select>

	<select id="getSelectStatistics" parameterType="java.lang.Integer" resultType="com.tce.smart.platform.core.vo.SelectStatisticsVO">
		select ANSWER ,count(ANSWER) as num
		from SMT_PAPER_RECORD
		where QUESTION_ID=#{id}
		group by ANSWER
	</select>

</mapper>
