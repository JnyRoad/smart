<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tce.smart.platform.core.mapper.energy.SmtEnergyIngestionLedgerMapper">
	<!-- Oracle 唯一索引在并发重投时负责仲裁，避免先查后插造成重复历史。 -->
	<insert id="insertIgnoreDuplicate">
		INSERT /*+ IGNORE_ROW_ON_DUPKEY_INDEX(SMT_ENERGY_INGESTION_LEDGER (SOURCE_EVENT_ID)) */
		INTO SMT_ENERGY_INGESTION_LEDGER
		(ID, SOURCE_EVENT_ID, EVENT_TYPE, PARK_ID, METER_SOURCE, METER_ID, EVENT_TIME, PAYLOAD_HASH, EVENT_PAYLOAD, INGESTED_AT)
		VALUES (#{id}, #{sourceEventId}, #{eventType}, #{parkId}, #{meterSource}, #{meterId}, #{eventTime}, #{payloadHash}, #{eventPayload}, #{ingestedAt})
	</insert>

	<select id="selectPayloadHash" resultType="java.lang.String">
		SELECT PAYLOAD_HASH FROM SMT_ENERGY_INGESTION_LEDGER WHERE SOURCE_EVENT_ID = #{sourceEventId}
	</select>
</mapper>
