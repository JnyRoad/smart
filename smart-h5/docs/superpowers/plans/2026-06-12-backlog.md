# backLog 审批域实施计划（backlog-live / backlog-approvals）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 Codex 确认的 spec（`../specs/2026-06-12-backlog-design.md`）交付审批域 10 页 + Spike 文档（已落档），两分支依次合并。

**Architecture:** 分支 1 落域公共层（`src/features/backlog/`：api / approval-tabs / staff-filter-popup / useIsSecurityGuard / code-status）+ 首页与生活区审批 + /code；分支 2 复用骨架做办公区/报修/退宿审批，并抽取批 2 的 FaceAvatar、RepairReplyList 为共享组件。

**Tech Stack:** 同前批（Next.js 16 · antd-mobile 5 · TanStack Query + useListPager · vitest · Playwright）

**通用约定：** 每 Task `pnpm check && pnpm test` + 对应 E2E；分支收尾全绿 → 子 agent 评审 → 修复 → 复评 → PR → 合并；E2E mock 形状先 grep 旧 Vue（`~/source/YUTO/yuto-smart/smart-h5/src/views-mobile/pages/backLog/`）。

---

## 分支 1：feat/backlog-live

### Task A1: 域公共层（TDD）

**Files:** Create `src/features/backlog/{api.ts,code-status.ts,approval-tabs.tsx,staff-filter-popup.tsx,use-is-security-guard.ts}`；Test `code-status.test.ts`

- [ ] **Step 1: code-status 失败测试**（spec §4 /code 五态：expire 最高优先 → status 1..5 各态；`codePanelState({expire:true,status:2})==='expired'`、status 2 无 qrCodePic 时仍为 'qr'？旧 code.vue 实现时核对——若旧版只看 status 则照旧）
- [ ] **Step 2: 实现 + api.ts**（spec §3 全部端点与类型；列表查询类型含可选 licensePlate 但 UI 不传）
- [ ] **Step 3: approval-tabs（label 可配的页内双 Tab，`?tab=done` 直达）、staff-filter-popup（字段可配：badge/name[/releaseItem][/startTime/endTime]，条件挂载每开即净）、useIsSecurityGuard（baseinfo isSecurityGuard===0）**
- [ ] **Step 4: 全绿 → commit**

### Task A2: /backlog 首页 + 生活区审批列表/详情

**Files:** Create `src/app/backlog/page.tsx`、`backlog/release-live/page.tsx`、`backlog/release-live/detail/page.tsx`

- [ ] **Step 1: 首页**（3 入口卡导航）
- [ ] **Step 2: 列表**（spec §4 字段/配色/保安搜索；点卡带 id+sort+tab）
- [ ] **Step 3: 详情**（批 3 live 详情骨架 + sort/isUploadImg/status 条件区 + 意见输入 + 双按钮组；GET status/update 与 POST security/update；失败回 /home）
- [ ] **Step 4: check+test → commit**

### Task A3: /code + E2E + 收尾

**Files:** Create `src/app/code/page.tsx`、`e2e/backlog-live.spec.ts`

- [ ] **Step 1: /code 页**（spec §4：免登录、五态、单据/状态/流转卡、tel: 链接）
- [ ] **Step 2: E2E**（spec §7 前三组 + 保安身份两态搜索按钮显隐 + 列表请求不带 licensePlate 断言）
- [ ] **Step 3: 全绿（含 build）→ 评审 → 修复 → 复评 → PR → 合并**

---

## 分支 2：feat/backlog-approvals（从最新 main 拉）

### Task B1: 共享组件抽取（行为不变）

**Files:** Create `src/components/face-avatar.tsx`、`src/features/dorm-services/repair-reply-list.tsx`；Modify `src/app/dorm-exit/detail/page.tsx`、`src/app/dorm-repairs/detail/page.tsx`（迁移引用，渲染不变，靠既有 e2e 回归保护）

- [ ] **Step 1: 抽取 + 迁移 → 既有 e2e 全绿 → commit**

### Task B2: 办公区审批 2 页

- [ ] **Step 1: 列表**（approvalStatus 双 Tab + releaseItem 下拉搜索）
- [ ] **Step 2: 详情**（批 3 work 详情骨架 + isUploadImg 上传区 + status===2 双按钮 POST security/update；成功跳本模块 `?tab=done`——修正旧 bug）
- [ ] **Step 3: check+test → commit**

### Task B3: 报修审批 2 页

- [ ] **Step 1: 列表**（approve/list/repairs/list recordType=5）
- [ ] **Step 2: 详情**（状态卡+信息卡+时间线+描述输入+RepairReplyList；status 0 接单/不接单 → 无参列表；status 1 维修结果 → `?tab=done`）
- [ ] **Step 3: check+test → commit**

### Task B4: 退宿审批 2 页（含 processRecord 归一化）

**Files:** +`src/features/backlog/exit-process.ts`（normalizer 纯函数 + 单测：processRecord ?? approvalProcess ?? []）

- [ ] **Step 1: normalizer TDD**
- [ ] **Step 2: 列表**（POST dor/quit/list/approval；保安搜索 badge/name；配色核对旧 list.vue）
- [ ] **Step 3: 详情**（FaceAvatar + 字段 + 归一化时间线 + 描述(50) + **isApprove===true 才显示按钮**；status 1→2/3、status 2→4/5；成功 `?tab=done`，失败回 /home）
- [ ] **Step 4: check+test → commit**

### Task B5: E2E + 收尾

**Files:** Create `e2e/backlog-approvals.spec.ts`

- [ ] **Step 1: E2E**（spec §7 后三组，含 isApprove 缺失只读高风险回归、办公区跳转修正回归）
- [ ] **Step 2: 全绿（含 build）→ 评审 → 修复 → 复评 → PR → 合并 → 更新记忆批次表 → 汇报批 4**

---

## Self-Review 记录

- spec 覆盖：§3→A1、§4→A2/A3、§5→B2-B4、§6 已落档、§7 测试逐条映射、§8 内嵌对应 Task。
- 占位扫描：code.vue 五态优先级「实现时核对」为既定净室动作（A1 Step1 明确了两种可能的处理）。
- 类型一致：codePanelState 与批 3 qrPanelState 分离命名，无冲突。
