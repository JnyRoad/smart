# 012 发布契约与验收记录

本文件随规格维护。当前属于本地实施记录，没有执行生产变更、发布或真实设备操作；最终完成状态以 [tasks.md](tasks.md) 和 [quickstart.md](quickstart.md) 为准。

## 数据库变更契约

| 对象 | 本地已验证的结构 | 发布前核对 |
| --- | --- | --- |
| `SMT_AUTH_OPERATION_BATCH` | 22 列；园区内幂等键唯一、稳定选择和意图指纹、展开游标及阶段时间 | 名称冲突、字符串长度、Oracle 版本、CLOB 绑定、实际容量 |
| `SMT_AUTH_DELETE_REQUEST` | 15 列；一个批次多个请求，来源身份及代次唯一；来源 7 允许原行号为空但保留身份快照 | 来源类型与现有业务归属、主体状态和后续版本保护 |
| `SMT_AUTH_OPERATION_TARGET` | 30 列；目标键及业务身份唯一；请求/批次/园区复合外键 | 领取与分页计划、后续原任务关联索引、资源及接入身份 |
| `SMT_AUTH_OPERATION_ATTEMPT` | 19 列；目标内尝试号唯一，持久租约与外部标识 | 在途恢复、旧尝试与新尝试隔离 |
| `SMT_AUTH_RESULT_EVENT` | 16 列；尝试内事件键唯一，事件/尝试/目标复合归属 | 事件重放、正文保留和本地收敛流程 |
| `SMT_AUTH_SUBJECT_COORD` | 5 列；永久主体自然键唯一，本地短事务序列化点 | 与来源/资源锁顺序、同主体竞争及分片事务边界 |
| `SMT_AUTH_SOURCE_COORD` | 18 列；稳定来源、永久代次、冻结意图、当前物理行与指纹 | 业务入口门禁、覆盖选择完整性、历史幂等和来源条件收敛 |
| `SMT_AUTH_RESOURCE_COORD` | 22 列；完整主体/接入/设备资源键、当前与已应用代次、未决尝试 | 同资源顺序、未知核验、多窗口能力与旧回执补偿 |
| `SMT_AUTH_SOURCE_RESOURCE` | 19 列；资源级意图、当前及历史绑定修订、旧证据补偿失效唯一键 | 批内共享与跨批计数、部分设备撤权表示、有界确认 |
| `SMT_AUTH_IDENTITY_ALIAS` | 15 列；按园区/接入/设备保存多代外部身份 | 旧任务迁移、歧义核验及卡号/人员号变化 |
| `SMT_AUTH_WORKFLOW_SHARD` | 8 列；批次外键、同起点唯一、STAGE/BIND 与规范分片指纹 | 与页业务写入及游标原子提交、重复游标内容核对 |
| `SMT_AUTH_SELECTION_SOURCE` | 32 列；冻结来源、旧新窗口、来源类型、主体类型、版本化业务快照与父来源引用；AUTH_ID可空 | 实际业务选择、受理容量、来源行精确CAS、旧STAFF v0与新family隔离 |
| `SMT_AUTH_SELECTION_RESOURCE` | 18 列；每来源物理资源和INCLUDE/EXCLUDE窗口，主体类型与版本化凭据CLOB，来源复合外键 | 历史设备完整性、分页恢复、物理目标去重和凭据适配 |
| `SMT_AUTH_TRANSPORT_PHASE` | 44 列；尝试与阶段唯一、外部配置及下载真实标识、明细游标、PERSON操作与身份证明，以及凭据版本/CLOB两列 | 阶段归属、未知提交、迟到标识登记、冻结凭据、人员共享证明和补偿 |
| `SMT_AUTH_DIRECT_CLAIM` | 13 列；设备/物理键类型/键值自然唯一、永久主体与资源归属、首个阶段外键 | 员工及非员工共同参与、原子多键、旧发送接管、1/7物理归一与真实设备线协议 |
| `SMT_AUTH_TRANSPORT_REVIEW` | 8 列；旧任务核验键、园区、接入、原因和状态 | 受保护旧任务的幂等核验、人工治理入口 |
| `SMT_AUTH_TRANSPORT_IDENTITY` | 7 列；实例与ISC人员ID联合主键，持久记录所属主体及首次阶段 | 历史身份导入、跨主体冲突核验、人工确认审计 |
| `SMT_AUTH_PERSON_OWNER` | 16 列；实例与主体自然键唯一、原PERSON阶段唯一且有外键，保留规范身份、操作、令牌与未知状态 | 旧身份保守导入、未知建人不转让、父配置失效与跨设备竞争 |
| `SMT_AUTH_SCHEDULER_STATE` | 9 列；实例主键、配置摘要和各通道秒窗额度 | 多进程共享额度、实际实例映射和限流参数 |
| `SMT_AUTH_SCHEDULER_ROUTE` | 3 列；园区与接入联合主键，实例外键 | 多进程路由冲突及显式迁移，不自动覆盖已有归属 |
| `SMT_AUTH_SCHEDULER_JOB` | 11 列；实例与通道主键、租约、延期和持久游标 | 公平轮转、故障隔离、实例外键和容量 |
| `SMT_AUTH_SCHEDULER_QUOTA` | 7 列；实例、园区、设备、范围及通道联合主键，实例外键 | 分层额度竞争、园区在途保护、批量提交与实际吞吐 |
| `SMT_AUTH_GOVERNANCE_ACTION` | 24 列；动作幂等键、操作者、期望版本与尝试、状态前后值、结果和受限证据CLOB | 重试门禁、审计保留、园区授权、人工观察与可信设备结果分离 |
| `SMT_AUTH_REQUEST_INTAKE` | 12 列；操作者与请求键唯一、规范请求指纹、最终回执、全园区子批次数及摘要 | 同事务受理、原键重放鉴权、完整子批次核验、浏览器未知请求恢复 |

物理字段与约束详见 [data-model.md](data-model.md)。可复现的合成测试基线位于 `smart-module/smart-platform/smart-platform-core/src/test/resources/auth-operation/operation-schema.sql`；既有表的合成基线是同目录 `legacy-schema.sql`。这些是测试资源，不能直接当作已审定的生产迁移，也不能用合成列宽覆盖现存业务表。

发布变更依据目标库实际元数据和本规格生成并记录已执行版本，不新建人工脚本目录。新增对象顺序为批次、请求、目标、尝试、结果事件及相关外键/索引；后续授权版本与来源对象必须一并落实后才能开启统一业务入口。不能只发布新 Java 类而漏掉数据库结构，或只建五张表就把可靠删除标为完成。

五张协调表的合成结构位于同目录 `version-schema.sql`，独立协调层及工作流限定复审已通过，实际入口和接入仍在整合审查。列数是本机测试库实际元数据，不能替代目标生产库变更核对；完整业务范围和容量尚未交付。

分片日志、调度额度的合成结构分别位于同目录 `workflow-schema.sql`、`scheduler-schema.sql`；员工选择、接入阶段及治理动作位于 `smart-module/smart-platform/smart-platform-core/src/test/resources/authoperation/selection-schema.sql`、`transport-schema.sql`、`governance-schema.sql`；请求头位于 `smart-module/smart-platform/smart-platform-core/src/test/resources/auth-operation/intake-schema.sql`。请求头增量后的最新元数据核对共24张AUTH表，列数如上；QUOTA主键/实例外键及IDENTITY主键均为ENABLED。ACTION有5个命名且ENABLED的约束，另有系统NOT NULL约束；实际查询到2个唯一普通索引。TRANSPORT_REVIEW的PARK_ID现允许NULL，用于历史归属无法确定的持久核验；管理入口必须具备明确鉴权的无园区核验路径，不能静默丢弃这些记录。保留的园区9001合成基线仍为10,000个目标，仅作基础数据库验证。

通用来源增量随后在暂停其他测试worker的独占窗口迁入：SOURCE由26增至32列，RESOURCE由15增至18列；SUBJECT_ID改为256 BYTE，新增kind/type为64 BYTE。来源稳定唯一键加入SOURCE_KIND；pending索引加入SUBJECT_TYPE，auth索引加入SOURCE_KIND，另加按园区、类型、稳定键及状态定位的索引。新增身份字段使用普通STAFF/v0默认值，DEFAULT_ON_NULL=NO；旧SQL省略新列可继续写员工投影，新通用Mapper显式NULL仍拒绝。迁移前后两选择表各0行，保护基线不变；新4例与旧员工7例真实Oracle通过并清理自有数据。这只证明迁移后的旧员工SQL兼容，不允许旧无类型worker与新family混跑；须升级全部相关worker后才能启用新family。

PERSON增量独占迁入PHASE三列与16列owner表，owner主键、两唯一及phase外键均ENABLED；补充按实例/主体/阶段查历史与按实例/状态分页、时间筛选的索引。固定28项Oracle通过，84个测试自有实例记录清零，保留基线未变。随后修复配置异常误走资产路径的P2，两个反例从失败转为通过，171项离线回归及限定复审通过。该修复不改SQL、结构或事务注解，28项Oracle是修复前的固定数据库基线，没有重复运行或冒充最新整合验收。

人员HTTP幂等受理头已在独占窗口迁入；先核不存在等价索引，再新增 `IX_AUTH_BATCH_SOURCE(SOURCE_ID,ID)`。固定3项Oracle通过：真实请求键竞争、跨园区受理整体回滚、旧来源及组删除后原键重放且核全园区权限与子批次摘要。测试自有数据归零，池关闭；后端81项和前端68项通过。前端两项跨页面请求键竞争问题随后修复，最新77项相关测试及限定复审通过；原生IndexedDB双标签页和真实Vue配合合成响应的恢复验证通过。真实接口和设备联调仍未执行。

员工历史证据最终增量126项离线与6项Oracle通过，限定复审关闭旧关系保留且设备迁园导致整批中止的问题。未新增DDL；四张旧表的原始语义和缺口证据使用已有版本化业务CLOB保存。测试6个池关闭，自有60项表计数为0，园区9001批次与10k目标全列哈希不变。全局历史盘点core随后完成离线首包，独立审查发现园区冲突分类、部分flow恢复和锁等待后租约判定三处问题，正在修复；两张新表尚未迁入，不能将此增量视为存量残留治理完成。

DIRECT claim基础包完成车牌禁字符修复及限定复审，16项离线与冻结6项Oracle通过、0跳过。独占窗口先核PHASE精确42列、新两列和CLAIM不存在，再追加NUMBER(10)/CLOB两列并创建13列CLAIM；五个命名约束ENABLED、两个辅助索引及PK/UK索引VALID。六项覆盖CLOB/v0读取、MANDATORY、两连接物理键竞争、第二键失败回滚、claim与INTENT共同回滚、移园不释放归属。自有六实例记录清零，池及线程退出，9001批次/请求/10000目标全内容hash未变。此为基础组件验证，实际begin/typed receipt与旧出口整合、STAFF raw7证据桥及生产切换仍待完成。ROUTE接管候选列仍未迁入，不在上表本地已验证列数中提前计入。

资源定位查询只在两个Mapper的四条SQL增加 `INDEX(C IX_ASR_RESOURCE)`，未新建索引或修改统计。6项Oracle语义与早解析后增长A/B通过：五种SQL形态均实际使用资源等值范围扫描；10k无关资源下laneTarget/executionOwner单次逻辑读443降为7。带after的audit在1000条历史修订分布中由47增为52次逻辑读，未声明所有分布都加速。发布前必须核实际schema存在可用的 `IX_ASR_RESOURCE(RESOURCE_COORD_ID,SOURCE_COORD_ID,SOURCE_GENERATION)` 且计划采用提示；提示被忽略不会报错，不能仅凭SQL部署成功认定性能达标。结合空页修复的10k复验完成展开，但1800秒只确认3742/10000，仍FAIL；下一增量分离物理回执阶段完成与跨设备业务来源收敛，保留全部业务删除门禁。

本机既有三表 `SMT_DEVICE_TASK`、`SMT_TASK_DOWN_RECORD`、`SMT_STAFF_DEVICE_AUTH` 的 ID 已使用专用测试序列默认值验证自动生成。它们不说明生产使用相同序列或 Oracle 版本；发布前仍须核对既有 ID 机制，不能把测试序列变更直接套到生产。

## 当前应用增量

- ISC 可信设备成功结果通过可代理事务完成器维护任务与本地下发记录，记录异常回滚；旧 ISC 批次和重复成功状态不能重复收敛。
- 平台人员不存在、无下发数据、仅有平台权限项均不作为设备成功；混合批次中的缺失员工跳过后续建人/人脸预处理。
- 有界删除轮次内复用身份查询，跨园区及跨轮隔离；保留各任务保存的可信身份与三态查询结果。
- 新批次基础的职责是持久受理、分片、尝试与证据；共享来源协调、原任务适配、管理入口和公平调度仍按任务清单逐项接入。
- 新接入优先使用本系统可信历史任务、记录及别名中的ISC人员ID；工号单次查询命中不足以证明历史归属。没有可信本地绑定的存量ISC人员进入 `ISC_IDENTITY_UNPROVEN` 核验，不能视为所有存量人员均已支持自动新增。上线前需要可信身份映射导入或经审计的人工确认；管理写入能力尚未完成。

## 发布与回退条件

在数据库和两类接入受控验证完成后，先对 ISC 选定园区启用统一入口，并记录设备型号、接入版本、限流与资源配置。万级验收分别记录受理、展开、排队、设备确认和本地收敛，不能把局部 SQL、Mock 或内存吞吐替代真实结果。

回退时先停止新增批次受理，保留已受理批次、尝试及证据，继续核验和收敛在途操作。已产生任务或设备副作用后，不能通过删新表、清队列或恢复旧的提前删除入口来回退。只在确认相关新增对象没有业务数据、没有外部执行后，才可按对应发布记录回收未使用对象。
