# 验证步骤

## 本地应用验证

从 `smart-module/` reactor 运行相关 JUnit 测试，先记录失败断言再实现。测试不得启动 Spring 全量业务连接或调用实际 ISC/设备。

```bash
mvn -pl smart-platform/smart-platform-core,smart-schedule -am -Dtest=ISCDeviceTaskServiceImplTest,SmtIscDownRecordServiceImplTest -Dsurefire.failIfNoSpecifiedTests=false test
```

第一阶段新增测试名称和实际结果由执行记录追加，不能用预期结果代替执行输出。

### 第一阶段已执行结果（2026-09-05）

- `IscTaskCompletionServiceTest` 与 `SmtIscDownRecordServiceImplTest`：13 项通过，失败/错误为 0。
- `ISCDeviceTaskServiceImplTest`：74 项通过，失败/错误为 0。独立审查发现并修复了新增权限的平台条目假成功、混合批次跳过标记未生效，并补齐完成器条件未命中、异常和下轮恢复的调用侧测试；限定复审通过。
- `IscTaskCompletionOracleTest`：本机真实 Oracle 4 项通过，未跳过，Maven 退出码 0。分别验证下发记录删除后异常导致任务和记录一起回滚、重读任务可重试收敛、旧 ISC 批次条件未命中、外层事务回滚时完成器不提前提交，以及两个执行者持有同一旧快照时恰好一次收敛。

上述四个测试类已合并运行：91 项通过，失败/错误/跳过均为 0；命令为 `mvn -pl smart-schedule -am -Dtest=IscTaskCompletionOracleTest,IscTaskCompletionServiceTest,SmtIscDownRecordServiceImplTest,ISCDeviceTaskServiceImplTest -Dsurefire.failIfNoSpecifiedTests=false test`，Oracle 凭据仅通过当前进程环境注入。

Oracle 用例使用真实 MyBatis Mapper、Spring 事务代理及合成任务/下发记录；与用例无关的设备资料和授权来源同步使用替身。该结果证明被测本地事务边界，不证明来源行删除、授权版本保护或设备实际权限已收敛。

环境诊断：首次 JDBC 登录失败时未进入事务断言；核对 SQLPlus、旧 12.2.0.1.0 JDBC 和独立合成账号后，确认临时库长口令与旧驱动不兼容。仅调整本任务测试账号口令后通过，未升级项目驱动或修改实际环境认证配置。

### 批内身份复用与持久基础回归（2026-09-05）

ISC 批内身份复用新增 4 项测试，隔离运行旧实现时有 3 项行为断言失败，修复后 78 项通过；独立审查通过。同一轮同园区同身份跨设备只远查一次，任务原有 `personId` 优先，查询失败不会跨轮永久缓存。

随后六个测试类合并 Maven 运行通过 112 项（core 24、schedule 88），失败、错误和跳过均为 0。除上述四类外，加入 `AuthOperationServiceTest` 的 11 项及 `AuthOperationOracleTest` 的 6 项。真实 Oracle 共 10 项，包含双连接同目标竞争、分片回滚、重复分片冲突、提交登记及未知结果保护。持久基础仍须独立审查；此结果不代表版本协调、业务入口、万级容量或真实设备验收完成。

### 版本基础与第一轮审查修复回归（2026-09-05）

九个测试类统一 Maven 运行通过 153 项（core 41、schedule 112），失败、错误和跳过均为 0。真实 Oracle 34 项由 `AuthOperationOracleTest` 17 项、`AuthOperationVersionOracleTest` 13 项和 `IscTaskCompletionOracleTest` 4 项组成。命令仍从 `smart-module/` 执行：

```bash
mvn -pl smart-schedule -am -Dtest=IscTaskCompletionOracleTest,IscTaskCompletionServiceTest,SmtIscDownRecordServiceImplTest,ISCDeviceTaskServiceImplTest,AuthOperationServiceTest,AuthOperationOracleTest,SmtTaskDownRecordServiceImplTest,AuthOperationVersionTest,AuthOperationVersionOracleTest -Dsurefire.failIfNoSpecifiedTests=false test
```

本轮验证冻结预期数不可降低、未知结果后继续接收可信回执、ISC/直连各自真实标识、提交重载事务回滚，以及永久来源/资源代次、共享来源并撤、历史别名隔离、精确有效期和旧结果补偿决策。直连下发记录保存或删除返回 false 时会抛异常，5 项错误传播测试通过；完整直连完成事务仍在实施。

153 项通过后，独立复审发现另一个未覆盖边界：目标被新尝试确认后，旧尝试迟到失败不应被提升为确认状态。该问题继续修复；本记录不代表持久基础已无审查阻断。版本基础尚未接入员工覆盖/重发/删除等业务入口，不能据这些测试认定设备残留已解决。

该边界随后完成第二轮修复和限定复审：终态目标的新事件保存后直接返回，历史尝试不变。`AuthOperationServiceTest` 19 项和 `AuthOperationOracleTest` 18 项统一 Maven 运行通过（共 37 项，失败/错误/跳过为 0）；Oracle 新用例真实验证第二尝试确认后，第一尝试的状态、确认时间和结果指针保持不变，迟到失败事件仍存在。T011/T012 的持久基础阶段据此完成，后续版本和来源整合仍待验证。

直连事务前置增量另通过 32 项回归：`DirectTaskCompletionServiceTest` 7、`IntergrationAccessControllerTest` 13、`DeviceTaskServiceImplTest` 6、`DirectTaskCompletionOracleTest` 6。真实 Oracle 验证任务条件更新、删除记录维护与派生写入同事务，以及重复回执、外层回滚和迟到受理保护；本阶段已通过独立审查；完整 T017/T018 仍待目标、尝试与来源版本接入。合成下发记录表缺自动 ID，新增记录曾报 ORA-01400；最终事务用例使用删除记录路径及显式合成派生 ID，没有证明实际新增记录或访客任务的数据库自动 ID 生成。

版本 Oracle 测试曾出现 ORA-12516，改用最多 4 连接的测试连接池后本轮通过；没有重启容器或调整 Oracle 进程参数。当前证据不足以认定此前瞬时连接失败的唯一原因。

## Oracle 前置与验证

旅途已授权本机临时 Docker Oracle 测试。当前专用容器为 `smart-auth-012-oracle`，PDB 为 `FREEPDB1`，合成 schema 为 `SMART_AUTH_TEST`，宿主端口仅绑定 `127.0.0.1:32768`。容器使用 2 CPU、3 GiB 内存；实际运行版本是 Oracle AI Database 26ai Free 23.26.3.0.0。此处版本与性能不能代表生产 Oracle。

测试基线位于 `smart-platform-core/src/test/resources/auth-operation/legacy-schema.sql`；批次模型测试表位于同目录 `operation-schema.sql`，来源/资源及别名协调表位于 `version-schema.sql`。仅包含本任务合成表和数据，不能作为生产元数据快照或直接发布脚本。核对字段、约束、索引与查询计划后，再验证条件领取、事务回滚、租约过期和并发授权。

`IscTaskCompletionOracleTest` 只在显式提供 `SMART_AUTH_ORACLE_URL`、`SMART_AUTH_ORACLE_USER`、`SMART_AUTH_ORACLE_PASSWORD` 时运行，并限制本机地址及专用 schema；普通单测默认跳过。密码只由当前进程环境传入，不写入文档、命令参数或配置文件。真实 Oracle 用例的结果另行记录，不用 Mapper 解析通过替代数据库事务验证。

## 双接入与容量

1. ISC 和直连分别验证多设备成功/失败/离线、未知及乱序结果、旧任务与新授权冲突。
2. 向业务入口在 60 秒内注入 10k、20k 去重设备目标，分别新增、覆盖/重发、删除；记录受理、展开、排队、确认与收敛耗时。
3. 两万新增积压中插入小批撤权，并同时处理回执及低优先级存量任务；核对保留配额与 SC-002。
4. 一万人乘十设备仅验证十万目标展开有界与崩溃续接；单独记录真实终端处理速率。
5. 报告硬件/ISC版本、网络、资源、设备分布、并发、批次、限流和在线率；分别列模拟、数据库集成、真实设备结果。

未执行数据库变更、真实设备写入或生产发布时必须在交付中保留该状态。

版本协调 fix2：PC8+真实Oracle20全部通过，独立限定复审通过；包括最后来源删除后迟到ADD补偿DELETE、锁后读取等待事务已提交的新代次。管理只读查询：PB14+真实Oracle2通过，实际分页插件生成COUNT及Oracle双层ROWNUM SQL；300个合成目标仅验证分页/count及当前页latest-attempt，不代表10k容量验收。

### 工作流组合与当前审查（2026-09-05）

正式 Maven 通过 142 项：foundation20、workflow4、foundationOracle18、workflowOracle18、ISC82，失败/错误/跳过均为0。最后新增 `submittedWithinTransaction` 的真实 Oracle 外层回滚用例另定向通过1项。ISC82包含园区与外部批次联合分组、避免人员明细巨大IN、失败重试旧状态/动作/批次CAS和单园区解析异常隔离。

工作流独立审查发现的两项阻断已修复并通过限定复审：缺少目标本地记录回调时保持未完成；同已消费游标仅允许规范内容一致的分片重放。fix1 最新隔离回归48项全部通过（foundation20、workflow4、真实Oracle24），未重跑本轮全reactor Maven。分片日志与业务页同事务，记录完成门禁覆盖共享复用及来源结算；整体入口、双接入和容量尚未验收，灰度继续关闭。

201资源串行用例的单次阶段耗时为：来源登记7530ms、目标绑定27559ms、领取1218ms、提交准备及登记16281ms、回执与来源收敛17242ms、目标结算1380ms，合计71.210秒。该数据用于定位后续批量优化，未执行10k/20k完整容量验收，没有据此承诺生产完成时间。

fix1 同用例最新单次阶段合计14.623秒。两次运行期间依赖及环境演进，不是受控性能对比，不能把差异归因于本次修复，也不代表万级容量通过。

### 员工入口、接入及界面阶段验证（2026-09-05）

员工入口冻结子阶段最初通过38项隔离测试与8项真实Oracle测试。fix1针对完整组锁顺序、设备缺失历史任务及旧删除门禁完成修复，最新47项隔离与13项Oracle通过，原4项P1限定复审通过；缺照片且零设备组合随后完成定向修复，2项隔离与2项真实Oracle RED→GREEN，限定复审通过。选择来源表26列、资源表15列；新增人员最小快照已真实写读。有界受理仍存在全量Java资源展开，离职、访客、车辆等入口尚未接入。

调度通过22项隔离及真实Oracle组合测试，另1项完整DIRECT受控调用链通过：真实员工撤权入口、Timer、调度、真实Mapper领取、外部受控发送、可信回执和来源收敛。两万候选仅验证公平分页，不能作为两万任务吞吐验收。跨进程路由归属约束、完整ISC管道和容量仍在补充验证。

管理GET的批次失败原因透传增量通过15项隔离测试及限定复审，未增加查询。权限任务界面fix2最新41项相关测试通过，路由切换后迟到写响应不会污染新选择，结果仍明确标识原权限组；限定复审通过。最新ESLint为0错误、68警告；439项全量前端通过是fix0历史证据，不代表最终分支重跑。后续生产模式构建及本地合成响应的浏览器验证通过，真实网关联调仍未验证。旧布尔写接口尚不返回批次标识，当前不能自动定位刚提交的批次。

完整ISC受控管道已暴露并推进跨事件重复ACK、正常撤权恢复抢占两类修复。新可信身份门禁曾因测试缺少可信历史绑定停在初始配置核验；补全合成历史记录及实际FACE_ADD协议后，两个ISC管道均已通过（见下）。调度QUOTA、身份IDENTITY及历史核验园区可空的合成前置已安装，元数据另见发布记录。10k/20k/100k容量仍未验收。

### 最新组合与容量探针（2026-09-05）

工作流 fix4 的双空来源身份与未发送 EXPIRED 等待结算通过4项定向真实Oracle测试及限定复审。等待例外要求明确 WAITING_RESOURCE_OWNER 标记且任务号、两种外部号、发送时间及所有结果痕迹为空；未知提交不得借此重发。完整最终工作流与正式reactor回归仍待执行。

| 场景 | 实际结果 | 证据边界 |
| --- | --- | --- |
| ISC共享来源与空明细两个管道 | 2测试，0失败/跳过，13.502秒 | 真实Oracle与受控Remote；覆盖分页恢复、重复ACK、最后来源撤权；非真实ISC设备 |
| 201人×1直连设备，完整删除 | 受理2635ms，展开9497ms，确认收敛66723ms；201 HTTP/ACK/CONVERGED，来源余量0 | 展开直接循环真实服务；后续发送使用调度，不代表Timer展开吞吐 |
| 1000人单次全量请求，仅受理 | 1052ms；1000冻结来源/资源，目标、phase、HTTP均0 | 单样本诊断，未展开/发送；不能证明P95/P99或T024容量达标 |

201探针采样最大堆约254MiB、连接4、在途20；1000受理采样最大堆约129MiB、连接1。采样值不是进程连续峰值，两次独立运行不能作为性能改善对比。201完整管道中来源登记和lane绑定各调用201次，调度lockState共4515次、累计约14.7秒；后续需核验实际Timer的持续展开及轮询开销。

上述旧版Timer展开每批一次只处理一个来源或lane，读到游标尾后下一轮重置；直接服务循环探针未覆盖这一节奏。后续实际Timer对照及修复结果见下。10k/20k完整新增/删除、100k展开恢复、积压中的小删除时延及ISC容量均未验收。

UI生产模式构建退出0，耗时111652ms；本地浏览器加载真实Vue页面及合成响应，验证批次分页、19位ID、零目标待核验显示0%、路由切换后的迟到写响应不污染新选择，页面错误捕获为0。临时预览与依赖链接已关闭清理；真实登录、园区权限、后端与设备仍为UNVERIFIED。

### 实际定时器展开反例（2026-09-05）

同一201人×1设备删除请求改由真实AuthOperationTimerTask按1秒fixed-delay调用。旧冻结调度版本在120个tick、120132ms后触发原展开截止：EXPANSION_CURSOR=60，EXPECTED=201，EXPANDED=0，target/phase/HTTP均0；201条业务来源仍保留。本次受理1111ms，失败发生在展开阶段。它与直接服务循环探针的差异验证了每两次tick仅推进一个来源的瓶颈，不能归因于设备发送配额。

此次运行1测试/1失败/0跳过；测试失败被如实保留。Timer及全部scheduler executor已等待退出，自有园区的13类业务和调度表核验余量均0，测试连接已关闭。

工作流最终冻结回归随后完成55项组合测试及2项迟到版本回执测试，全部通过；输入为工作流fix3固定依赖覆盖fix4两文件，不包含最新全reactor。Transport fix3的并发赢家保护经159项隔离测试和限定复审通过；真实Oracle新竞争场景尚未执行。Scheduler fix3的39项及DIRECT管道1项通过，复审另发现专用Feign Client全局Bean影响其他服务路由的配置回归，正由独立修复处理。

专用Feign Bean隔离问题随后完成fresh fix4与限定复审：真实Spring/OpenFeign普通服务路由在启用/禁用配置下均先复现误投dispatcher，修复后3项通过，固定旧客户端回归8项通过。专用连接池由非Client类型资源Bean托管，保持其他Feign的原有负载均衡客户端，容器关闭时回收专用池与计时器。

持续展开增量另通过44项组合测试与DIRECT管道1项：默认每工作项32步、每批轮转片4步、500ms启动检查点，独立新增/删除游标，持久stage→bind→finish辅助阶段。当前数据库调用不会被500ms检查点强行打断。配置上限、跨重启单步推进及来源封口门禁已局部验证，并通过限定复审。

同一201人×1直连设备的真实Timer用例随后通过：展开18705ms，管道143613ms，受理至首次HTTP21989ms、全部完成144913ms，共143次实际tick；201个目标及批次全部CONVERGED，201次HTTP及ACK，来源余量0。固定输入包含Scheduler fix3、持续展开、HTTP Bean fix4及Transport fix3；未混入仍在改动的通用来源和治理源码。JVM堆上限768MiB、连接池4，Oracle容器2 CPU/3GiB；自有13类表清理归零，线程及连接池关闭。该结果为真实Oracle与受控远端，不是真实设备性能。

本次最大在途23符合设备总上限25减去新增保留2。当前设备总HTTP额度8/s中的DELETE份额为3/s，园区40/s中的DELETE份额为15/s。实际发送仍受“领取准备一轮、发送一轮”的tick切换影响，正在补同tick内有界连续推进。即使消除此空转，10k/20k分布到20设备时仅园区配额的稳态下界也分别约667/1333秒，不能据总额度估算更短时间。探针120秒展开与600秒确认上限是诊断截止，不是规格承诺；SC-002的小批撤权时延门槛与SC-003的受理/列表门槛仍独立有效。

治理首个增量已通过23项隔离测试和3项真实Oracle测试，覆盖完全未发送尝试的精确重试、动作与状态同事务回滚、园区/全局分页分权，以及人工ABSENT仅留审计。Oracle后收紧敏感证据校验，再次通过23项隔离测试；冻结版本正在独立审查，未以此勾选完整T022。

通用来源前置随后完成27项离线测试及4项新增、7项旧员工真实Oracle测试。迁移后选择来源32列、资源18列；覆盖无authId、19位字符串、业务CLOB、微秒窗口、不同主体类型的同数字ID、旧员工默认v0与显式NULL拒绝。真实Spring代理测试验证父类型唯一注入与内层stage提交、外层绑定回滚后的同分片重放，但其事务管理器为测试替身；多来源跨页崩溃恢复还未取得真实Oracle证据。限定复审发现旧窗口CAS回归测试提前返回，补齐PENDING前置及真实deleteExact调用断言后27项通过，原问题已关闭；family业务入口和typed transport尚未完成。

### 有界连续推进与万级绑定反例

连续推进增量保持原线程、连接、速率和在途上限，每个工作项最多8个独立单元、500ms启动下一单元检查点。52项组合测试、1项DIRECT管道及1项新增窗口UNKNOWN排除测试通过，并经限定复审。混合组中已持久核验的未知成员保持占用在途，健康成员仍能推进；未放宽未知重发门禁。

| 固定输入场景 | 实际结果 | 结论范围 |
| --- | --- | --- |
| 201人×1直连设备，DELETE/TIMER | 受理1749ms，展开19661ms，管道88745ms；201 HTTP/ACK，目标及批次CONVERGED，来源0 | 相同数量的上一持续展开版管道为143613ms；受控远端，非真实设备吞吐 |
| ISC两个完整受控管道 | 2项通过，6.623秒，0失败/跳过 | 共享来源、配置/下载及明细回执行为；未记录的分阶段HTTP次数不补造 |
| 201人×1 ISC设备，DELETE/TIMER | 受理1547ms，展开16690ms，管道52956ms；201 ACK/CONVERGED，来源0 | 配置、进度、下载、明细各9次，共36次HTTP；每类8组23人及1组17人 |
| 500人×20 ISC设备，10,000目标，DELETE/TIMER | **失败**：900秒展开诊断截止；停止并等待在途数据库工作结束后仅250个PREPARING目标，cursor10250，500来源仍保留，HTTP0 | 单次受理1817ms；不能证明P95/P99，不能声明万级容量通过 |

10k失败的MyBatis层累计计时中，lanes调用250次、累计647311ms、单次最大8444ms；恢复候选调用1788次、累计110933ms。当前逐lane从头查找待绑定资源是已测主导成本；这些计时包含ORM与结果处理，尚无实际执行计划证明物理N²扫描。来源封口核验累计耗时较低，不能凭源码猜测就删掉代次门禁。后续优化先依据计划减少重复候选查询，再用同一固定容量场景复测。

上述四次运行均已停止驱动与调度线程、关闭连接池并清理自有数据；10k的原失败报告保留，未改断言、配额或诊断截止来获得通过。20k暂不继续放大，先修复已定位瓶颈；100k、SC-002小删除时延、SC-003正常列表与足量受理分位数仍未验证。

治理增量一随后完成两轮限定修复：证据准入、HTTP 409保留及规范值入库问题均关闭。最终30项离线及最新冻结生产代码的3项Oracle通过，后者耗时1.596秒，0失败/跳过；人工观察仍只记审计，不替代可信设备证据。人员删除/清空新增回执入口和界面定位批次已完成56项Java与58项相关UI测试及限定复审；其中修正PERSON权限组实际枚举为1，原错误值2属于VISITOR。HTTP请求幂等受理头正在独立增量实现，不能由回执接口的通过推导幂等已经完成。

ISC跨设备PERSON身份共享增量通过169项离线和28项Oracle，20设备受控反例由20次PERSON改为1次PERSON、19次独立FACE、1次GET。独立审查随后发现并修复配置异常误走资产路径的问题，171项离线和限定复审通过；没有改SQL或结构，28项Oracle保留为修复前固定基线。照片引用可覆写，因此没有跨attempt共享FACE。各增量仍需最终整合回归，真实ISC/直连设备与生产验收未执行。

请求级受理头增量已完成12列及完整子批次查询索引的临时Oracle迁移，81项后端、68项前端与3项Oracle通过。真实数据库覆盖唯一键竞争后重放、跨园区整体回滚、来源及权限组删除后重放与全范围权限核验。前端两个跨页面原键竞争问题随后修复，77项相关测试及限定复审通过。最终冻结代码在两个真实浏览器标签页使用原生IndexedDB验证：同意图复用原键、不同意图互斥、迟到400/409不清除新键、旧localStorage迁移完成后不复活。真实Vue页面配合合成响应验证清空请求响应丢失、刷新和原键恢复。浏览器证据不替代真实后端与设备联调。

针对10k失败的候选查询，保持业务门禁、索引、统计和配额不变，改写为同批选择资源与已绑定贡献资源的集合差。早解析再增长至10k资源的12组对照有序结果一致，原慢查询约2.2–2.6秒，MINUS为4–29毫秒；不同查询形态和解析历史仍分别记录。27离线与6Oracle边界测试通过、限定复审通过。SQL局部提速不代替完整展开、外部提交、回执和来源收敛验收。

通用来源与MINUS组合后的正式10k ISC DELETE/TIMER仍失败：900138ms时9118个目标，停止驱动并等待后为9127/10000，全部PREPARING，500来源保留，HTTP0；1测试、1失败、0跳过。自有13类表归零，池及线程关闭。该输入还包含通用来源生产增量，不能把旧250到9127的全部变化归因于一条SQL；20k继续暂停。恢复候选累计110936ms，四条资源定位查询合计调用131270次；跨线程累计时间不直接等于墙钟节省。

精确游标核对显示laneTarget/executionOwner使用非资源前导索引全扫描，current/audit使用唯一索引跳跃扫描；算子实际行数未记录。五份SQL_FULLTEXT与冻结BoundSql一致，计划中的NVL表达式不代表源码漂移。随后空页停止增量通过36项实际执行测试与限定复审，另1项Oracle按既有条件跳过；它保留非空两页预算和其他扫描类别。四条局部索引提示通过6项Oracle语义测试、增长后的9阶段ABBA访问路径核验及限定复审；10k无关资源时lane/owner每次逻辑读从443降到7，历史1000行auditAfter从47升到52的局部退化保留记录。

上述三个生产文件变化后的正式10k ISC DELETE/TIMER已重跑：500人×20设备，524510ms完整展开10000目标，首次HTTP距受理527064ms；原1800秒截止仍失败，最终3742 CONFIRMED、6143 QUEUED、115 WAITING_CONFIRM，500条来源保留。2729次HTTP含169次配置、169次进度、169次下载与2222次明细。真实明细共曝光50504条，幂等事件仅新增3742条；固定源码的设备阶段结束依赖target CONVERGED，使已确认设备在等待同来源其他设备时继续轮询。该因果已进入定向反例与修复，尚不能把诊断写成修复验收。1测试、1失败、0跳过，进程/线程/连接池已退出，自有13表归零，基础园区10000目标保留；20k不继续放大。

员工历史证据增量最终126项离线、6项真实Oracle及限定复审通过。覆盖四张旧表原始service 1/2/7、记录或设备缺失、512字符标识、证据CLOB、设备迁园/园区为空但旧关系仍在，以及同请求健康员工继续。疑点按员工/园区保留待核验；显式新增或替换到越权设备仍拒绝。该结果不是全局历史盘点或真实设备残留清除验收。

物理结算增量随后完成有效并发反例与修复：两个真实Oracle会话都先读到ACCEPTED，旧实现仅该用例CAS冲突失败；修复后8项实执行全部通过、0跳过、5.294秒。只有可信设备事件及本地record完成，或真实持久的旧代次观察，才结束本phase；全部共享来源逐条核实，target/source/request原终态门禁保留。并发CAS0只允许精确同一执行坐标的已完成phase幂等重读，正常CAS1无新增查询。8项纯原容量依赖离线与限定独立复审通过；新固定容量输入仍仅三Java变化，268项不变，900/1800秒门槛保持，尚待万条复验。

STAFF canonical1与完整History V2增量完成142项离线验证及限定复审，修复任务/下发记录时间编码与园区来源标签错配可被当完整证据的问题；旧SQL空值和未采集字段严格区分。新Oracle专项、历史快照分页上限及raw7身份/任务/记录桥仍待完成。SLO驱动两处计量缺陷也经14项离线和独立反例验证关闭：错误线协议身份不能计为成功，异步关闭期间异常仍使最终测试失败；SC002/003本身尚未运行。

最终批量分片档在同一类固定输入完成复跑：500人×20 ISC设备、10,000个DELETE目标，临时Oracle与受控ISC协议端在1,231,086ms内完成全链路，487,368ms完成展开，首次HTTP距受理495,326ms。全部10,000目标为CONVERGED，ISC_CONFIG=CONFIRMED、ISC_DOWNLOAD=FINISHED，来源余量0；配置、进度、下载、明细各60次请求，共240次。每台设备500人实际发为200、200、100三包，20台设备对应每阶段40包200人、20包100人；峰值堆296,170,720字节、连接4、实际在途400。该档使用显式ISC高峰配置：单请求200人、实例/园区在途2000、每设备220且新增与删除反向各预留20，调度32个连续工作项/1000ms、展开200步/1000ms。受控协议验证了账目、分片、回执和本地删除门禁，不代表真实ISC/直连设备或生产容量。

SC-002 的受控批量场景已补测：100人×200 ISC设备形成20,000个ADD目标并保持未确认积压；通过同一 MVC 请求提交20人、每人1设备的撤权，20个DELETE目标均在5,581ms获得首次ISC配置请求。此刻19,800个ADD目标仍在QUEUED，新增没有抢在删除完成前提交。该用例真实经过来源展开，但只覆盖单个批量撤权请求。20个独立撤权请求、每人200设备（4,000个DELETE目标）的反例在120秒内没有首次外发，故多独立请求的来源展开并发、20k完整新增确认、SC003正常列表、SC005混合故障、JVM崩溃恢复与现场联调仍未验收。
