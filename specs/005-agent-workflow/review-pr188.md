# PR #188 审查修订

本记录对应 [PR #188](https://github.com/JnyRoad/smart/pull/188) 对提交 `fa54704370cfef4dc12b0daa99f88ec91b71197d` 的首轮审查。仅修正已核实的工具契约和资料表述；不修改 Skills 或业务代码。

| 意见 | 核验及处理 |
| --- | --- |
| 任务格式可能重置完成状态 | 命令中的“每项任务”与 `[ ]` 示例存在歧义，改为仅新任务使用未完成标记，已有 `[x]` 保留；模板示例同步限定为新任务。 |
| Issue 标题中的任务 ID | 使用匹配到的完整 ID，标题格式改为 `<task-id>: <description>`，明确支持 `T1234`，避免把 `T001` 示例读成固定值。 |
| 计划中的旧 worktree | 文首已标记历史范围，进一步在原分支引用处标明 PR #179 历史阶段，并指出当前任务分支；不把历史事实改写成当前事实。 |
| 验证范围不完整 | 开头明确包含 Spec Kit 命令、workflow、integration manifests 和规格记录。 |
| 授权主体及时间 | 用户旅途是本次授权主体；准确日期及范围引用[授权记录](validation-20260905.md#提交前验证快照)，不采用删除主体的建议。 |

自主复核另发现公共 hook 契约遗漏“实际调用”：补回完整的 `hooks.before_<command>` / `hooks.after_<command>` 路径，以及 mandatory hook 输出标记后实际执行、等待完成的要求。hook 同样受本命令只读、产物及已有授权边界约束。

受影响的命令与模板摘要同步更新到 integration manifests。验证使用现有脚本、摘要、链接及差异检查；完整 Agent 工作流、外部 hook、真实 GitHub issue 创建和生产操作未执行，不把静态契约检查写成端到端通过。首次提交前的验证边界见[验证结果](validation-20260905.md#验证结果)；本轮已实际监控 PR，最新审查与合并状态以 PR 为准。

## 第二轮审查

对应提交 `f74eea2d005d018c4bd3e2594c89a086fa3efe96` 的 4 条行内意见及 1 条范围外意见：

- 任务模板的开发政策引用改为完整的仓库相对路径。
- 授权记录和验证边界使用上面的权威记录链接，避免另维护一套副本。
- YAML 错误改为显式报告诊断，但保留跳过扩展检查后继续命令的原生兼容语义；未采纳新增全命令阻断门禁的建议。
- 条件 hook 明确由当前命令跳过且不发出执行标记，交由外部 HookExecutor 求值和调度；未新增 true/false/unknown 回调协议。

后两项依据是[修改前的原生命令契约](https://github.com/JnyRoad/smart/blob/8c6e9f6c4def70da08eaafed9c482f7666721b96/.specify/commands/speckit.plan.md#L28-L38)：本次保留工具接口，消除交接歧义，不扩大为扩展运行时重设计。已有项目权限和验收仍由各自权威规则约束，不把跳过 hook 视为通过验收。
