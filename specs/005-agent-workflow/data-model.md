# 状态与资料关系

本次无业务数据模型和数据库迁移。

- 一个开发任务绑定一个工作区、一条任务分支和一个已有或新建的规格目录。
- Git 分支名称与规格目录名称独立；规格通过显式目录绑定。
- 规格、计划、任务、设计和验收资料持久化；当前功能指针、缓存和日志属于本机状态。
- detached worktree 先从当前 HEAD 绑定分支；main、共享目录和其他任务目录不能进入本任务写入阶段。
