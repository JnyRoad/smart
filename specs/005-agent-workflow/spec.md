# Feature Specification: 项目规则与开发资料持久化

**Feature Branch**: `docs/simplify-ai-instructions`（首次实施分支：`chore/project-agent-workflow`）
**Created**: 2026-09-04
**Status**: Approved
**Input**: 旅途已确认规则路由、独立 worktree、规格复用及资料入库，并在本次审计后批准非 Skill 指令精简；本轮范围见文末修订。

## User Scenarios & Testing

### User Story 1 - 换工作区仍能取得开发资料 (Priority: P1)

旅途在新的工作区继续任务时，可以取得已提交的规格、设计和验收资料，无需重新生成。

**Independent Test**: 从提交生成干净检出，核对两组门锁文档共 21 个文件以及已有规格均存在；本机指针和缓存仍不入库。

**Acceptance Scenarios**:
1. 已有本地门锁资料，首次纳入版本管理时保留完整且内容相同的 21 个文件；后续经核实的修订保留在 Git 历史中，新检出可取得修订后的资料。
2. 已有功能规格但没有本地指针时，明确绑定原规格目录，继续任务，不创建重复功能。

### User Story 2 - 每个开发任务有独立工作区 (Priority: P1)

Agent 在首次修改文件前确认本任务的工作区和分支，保护 main 和其他任务的改动。

**Independent Test**: 对照 main、共享目录功能分支、已有任务 worktree、detached worktree、旧任务 worktree 五种状态，规则给出明确且互不冲突的动作。

**Acceptance Scenarios**:
1. 在 main 或共享主目录开发时，先建立独立工作区和任务分支，再写规格与代码。
2. 已有本任务 worktree 和分支时直接复用；detached 时从当前 HEAD 建分支，不重新创建 worktree。
3. 创建隔离失败时停止依赖写入，不退回 main 开发，不自动处理其他任务的脏文件。

### User Story 3 - 按需加载一致的规则 (Priority: P2)

Agent 从根规则获得必须遵守的边界，再按任务读取具体流程，不依赖某台机器的个人规则。

**Independent Test**: 根路由链接全部可解析；项目工作流在规格分析后交给执行技能，不调用被禁止的实现入口。

**Acceptance Scenarios**:
1. 开发、规格续作与只读评估分别有明确加载范围。
2. 工作流和命令模板引用项目验证标准；行为变更具有对应回归证据，低风险纯文档变更使用链接、内容和差异检查，不强制测试与实现的先后顺序。

### Edge Cases

- 主目录中原有 `.superpowers/sdd/task-2-report.md` 删除不属于本任务，不恢复、不提交该删除。
- 目录被忽略不代表已有文件未跟踪；分别核对忽略、索引、HEAD 和远端。
- 同一分支被其他 worktree 占用时，不强制重复检出或自动切换他人目录。
- worktree 不隔离外部服务、数据库和端口；业务文档中的规划不视为当前实现。

## Requirements

### Functional Requirements

- **FR-001**: 持久规格、设计、计划和验收资料必须允许进入版本管理；本机状态继续精确忽略。
- **FR-002**: 开发首次写入前必须同时具备独立 worktree 和本任务非 main 分支。
- **FR-003**: 已有本任务工作区、分支和规格必须优先复用。
- **FR-004**: 根规则必须保留硬约束，并按任务路由到三个详细规则文件。
- **FR-005**: 项目工作流必须移除自动实现路径，并将分析通过的任务清单交给执行技能。
- **FR-006**: 对代码行为改动提供有意义的行为回归验证；文档与低风险配置改动使用适当检查，不以先写测试或先写实现作为验收条件。
- **FR-007**: 所有本次新增资料及规则按 `AGENTS.md` 中提交、推送和创建 PR 各自的既有授权，通过功能分支交付；已覆盖的授权不重复询问，未获对应授权不执行该动作，不自动合并。
- **FR-008**: PR 审查意见逐条对照当前代码与既有证据核实；只修正有效的规则、文档和参考原型问题，记录未采纳理由，不以设计资料修订代替业务实现。

## Success Criteria

- **SC-001**: 两组门锁资料 21 个原文件均能从提交取得；首次导入提交与原始资料逐文件一致，后续修订可由 Git 差异及审查记录追溯。
- **SC-002**: 工作区决策的五类状态均有明确动作，不允许 main 或共享主目录开发。
- **SC-003**: 规则路由没有断链，持久资料不再命中整体忽略，本机指针仍被忽略。
- **SC-004**: 工作流相关检查通过；已有规格可在没有本地指针的干净检出中只读解析。
- **SC-005**: 9 条 PR 审查意见均有判定及依据；确认的原型行为缺陷有先失败后通过的回归验证，文档修订通过一致性和链接检查。

## Assumptions

- 采用用户上一轮批准的轻量拆分，不引入新服务或通用规则引擎。
- 首次实施不修改业务代码、部署环境、全局个人规则或历史规格；后续修订范围见下文。
- 门锁资料首次导入保持原文；按用户本次审查修复授权纠正确认问题，保留原始提交历史。涉及外部仓库的引用仅作为已有资料来源，不宣称可独立复现外部系统。

## 2026-09-05 指令精简修订

旅途已批准按只读审计结果优化非 Skill 长期指令，删除 `smart-ui/.cursorrules`；精简实施阶段不修改任何 Skill、业务源码、真实数据库或部署环境，先交付本地改动。后续 PR 授权见下文；此前实施与 PR 审查记录保留为历史。

- **FR-009**: 项目事实、权限边界、工具参数和具体验收要求保留；同一维护范围内每项语义指定一处权威来源，其余使用引用。
- **FR-010**: 删除不保留人工 SQL 目录、绝对测试先行、每函数/每步骤注释、机械命名与拆分、规则冲突取更严、把规划当现实、重复审批、三轮无变化停止监控、无提交归属的 reaction 通过和自动清理清单要求。
- **FR-011**: 模块知识和命令集中到现有 README/项目文档；移除非 Skill 中的通用训诫、固定推理流程及重复内容，保留 Spec Kit 工具配置、格式接口、规格复用与只读参数。
- **FR-012**: 全局文件先生成可核验差异和原文备份，再按本轮授权写回；Skills、其他任务的改动及未跟踪门锁规划保持不变。
- **SC-006**: 八组指定问题均有处理记录；A 类保护可沿入口引用找到，B 类知识未丢失，C 类冗余已从非 Skill 活跃指令中清理。
- **SC-007**: 现有 Spec Kit 相关检查、链接与差异检查通过；Skills 校验摘要不变，项目改动仅位于本任务 worktree；全局写回后与批准范围内草稿一致。

## PR 交付授权

旅途随后明确授权提交、推送并创建本次 PR，持续监控，修复有效问题后重新提交，满足合并条件后合并。目标为 GitHub `JnyRoad/smart` 的 `main`；此授权不扩展到部署、同步其他远端或修改 Skills。合并前核对最新 HEAD 的检查、审查意见和可合并状态；本机全局规则不包含在仓库 PR 内。
