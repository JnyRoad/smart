# 认证服务调用 UPMS 内部登录投影时必须使用自身的 server scope 客户端凭据。
security:
  inner:
    # 登录投影调用使用独立 client_credentials，不能继承或转发终端用户令牌。
    service-token:
      client-id: "${SMART_AUTH_OAUTH_CLIENT_ID:}"
      client-secret: "${SMART_AUTH_OAUTH_CLIENT_SECRET:}"
      access-token-uri: "${SMART_AUTH_OAUTH_TOKEN_URI:}"
  oauth2:
    client:
      client-id: "${SMART_AUTH_OAUTH_CLIENT_ID:${SMART_OAUTH_CLIENT_ID:}}"
      client-secret: "${SMART_AUTH_OAUTH_CLIENT_SECRET:${SMART_OAUTH_CLIENT_SECRET:}}"
      scope: server

# 数据源
spring:
  datasource:
    type: "${SMART_AUTH_DB_TYPE:com.zaxxer.hikari.HikariDataSource}"
    driver-class-name: "${SMART_AUTH_DB_DRIVER:oracle.jdbc.driver.OracleDriver}"
    username: "${SMART_AUTH_DB_USERNAME:${SMART_DB_USERNAME:}}"
    password: "${SMART_AUTH_DB_PASSWORD:${SMART_DB_PASSWORD:}}"
    url: "${SMART_AUTH_DB_URL:${SMART_DB_URL:}}"

  freemarker:
    allow-request-override: false
    allow-session-override: false
    cache: true
    charset: UTF-8
    check-template-location: true
    content-type: text/html
    enabled: true
    expose-request-attributes: false
    expose-session-attributes: false
    expose-spring-macro-helpers: true
    prefer-file-system-access: true
    suffix: .ftl
    template-loader-path: classpath:/templates/
